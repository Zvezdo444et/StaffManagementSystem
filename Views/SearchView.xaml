<UserControl x:Class="Inspector.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:behaviors="clr-namespace:Inspector.Views.Behaviors"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000">

    <UserControl.Resources>
        <CollectionViewSource x:Key="GroupedParameters" Source="{Binding Parameters}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="Category" />
            </CollectionViewSource.GroupDescriptions>
        </CollectionViewSource>
        <Style x:Key="ParamCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="{StaticResource PagePadding}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
              Margin="0,0,0,12"
              Visibility="{Binding IsFiltersPanelVisible, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Поиск работников"
                       FontSize="28"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Экспортировать выбранное в Excel"
                    Height="44"
                    Padding="24,0"
                    Background="{StaticResource AccentGreenBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Command="{Binding ExportToExcelCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}" />
        </Grid>

        <Grid Grid.Row="1"
              Margin="0,0,0,12"
              Visibility="{Binding IsFiltersPanelVisible, Converter={StaticResource BoolToVis}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="White"
                    CornerRadius="12"
                    Padding="16"
                    BorderBrush="{StaticResource BorderLightBrush}"
                    BorderThickness="1"
                    MaxHeight="280">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Source={StaticResource GroupedParameters}}">
                        <ItemsControl.GroupStyle>
                            <GroupStyle>
                                <GroupStyle.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   Margin="0,12,0,6" />
                                    </DataTemplate>
                                </GroupStyle.HeaderTemplate>
                            </GroupStyle>
                        </ItemsControl.GroupStyle>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,2,0,6">
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              Content="{Binding DisplayName}"
                                              Style="{StaticResource ParamCheckBoxStyle}" />
                                    <StackPanel Margin="24,4,0,0"
                                                Visibility="{Binding IsSelected, Converter={StaticResource BoolToVis}}">
                                        <TextBox Text="{Binding FilterValue, UpdateSourceTrigger=PropertyChanged}"
                                                Style="{StaticResource SearchTextBoxStyle}"
                                                Height="36"
                                                materialDesign:HintAssist.Hint="Значение для фильтра"
                                                Tag="{Binding FilterType}" />
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Column="2" VerticalAlignment="Top">
                <Button Content="Очистить"
                        Command="{Binding ClearCommand}"
                        Height="44"
                        Padding="24,0"
                        Background="{StaticResource AccentGreenBrush}"
                        Foreground="White"
                        BorderThickness="0"
                        HorizontalAlignment="Left"
                        Style="{StaticResource MaterialDesignRaisedButton}" />

                <TextBlock Text="Шаблоны поиска"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,20,0,8" />
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="140" />
                        <ColumnDefinition Width="8" />
                        <ColumnDefinition Width="140" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBox Grid.Row="0" Grid.Column="0"
                             Text="{Binding NewTemplateName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource SearchTextBoxStyle}"
                             Height="40"
                             materialDesign:HintAssist.Hint="Название шаблона"
                             VerticalContentAlignment="Center" />
                    <Button Grid.Row="0" Grid.Column="2"
                            Content="Сохранить шаблон"
                            Command="{Binding SaveTemplateCommand}"
                            Height="40"
                            Padding="8,0"
                            Background="{StaticResource AccentGreenBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Style="{StaticResource MaterialDesignRaisedButton}" />
                    <ComboBox Grid.Row="1" Grid.Column="0"
                              Margin="0,8,0,0"
                              ItemsSource="{Binding TemplateNames}"
                              SelectedItem="{Binding SelectedTemplateName}"
                              Style="{StaticResource SortComboBoxStyle}"
                              Height="40"
                              materialDesign:HintAssist.Hint="Выберите шаблон" />
                    <Button Grid.Row="1" Grid.Column="2"
                            Margin="0,8,0,0"
                            Content="Загрузить шаблон"
                            Command="{Binding LoadTemplateCommand}"
                            Height="40"
                            Padding="8,0"
                            Background="{StaticResource AccentGreenBrush}"
                            Foreground="White"
                            BorderThickness="0"
                            Style="{StaticResource MaterialDesignRaisedButton}" />
                    <Button Grid.Row="1" Grid.Column="4"
                            Margin="0,8,0,0"
                            Content="Удалить шаблон"
                            Command="{Binding DeleteTemplateCommand}"
                            Height="40"
                            Padding="8,0"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Foreground="{StaticResource AccentRedBrush}"
                            BorderBrush="{StaticResource AccentRedBrush}"
                            HorizontalAlignment="Stretch" />
                </Grid>
            </StackPanel>
        </Grid>
        <TextBlock Grid.Column="0" Grid.Row="2"
                       Text="{Binding ResultsCount, StringFormat='Найдено: {0} записей'}"
                       VerticalAlignment="Center"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       Margin="4,0,20,0" />

        <Button Grid.Row="2"
                Content="{Binding ToggleResultsButtonText}"
                Command="{Binding ToggleResultsCollapsedCommand}"
                HorizontalAlignment="Right"
                Margin="0,0,0,6"
                Padding="12,6"
                Style="{StaticResource MaterialDesignOutlinedButton}" />

        <Border Grid.Row="3"
                Background="White"
                CornerRadius="12"
                Padding="0"
                BorderBrush="{StaticResource BorderLightBrush}"
                BorderThickness="1"
                Margin="0,0,0,0">
            <Grid>
                <DataGrid x:Name="SearchResultsGrid"
          ItemsSource="{Binding ResultsTable}"
          behaviors:DataGridShiftWheelScrollBehavior.IsEnabled="True"
          AutoGenerateColumns="True"
          IsReadOnly="True"
          CanUserAddRows="False"
          CanUserDeleteRows="False"
          HeadersVisibility="Column"
          GridLinesVisibility="Horizontal"
          MinRowHeight="30"
          ColumnHeaderHeight="36"
          BorderThickness="0"
          Background="White"
          AlternatingRowBackground="#FBFBFC">

                    <DataGrid.Resources>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap" />
                            <Setter Property="TextTrimming" Value="None" />
                            <Setter Property="HorizontalAlignment" Value="Left" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Padding" Value="8,4" />
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Margin" Value="8,0,6,0" />
                            <Setter Property="Padding" Value="8,4" />
                            <Setter Property="VerticalContentAlignment" Value="Center" />
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                                            <ContentPresenter VerticalAlignment="Stretch" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>
                </DataGrid>
                <TextBlock Text="Выберите параметры — результаты отобразятся здесь"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ResultsTable}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
    
</UserControl>
