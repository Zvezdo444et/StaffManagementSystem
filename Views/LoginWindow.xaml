<Window x:Class="Inspector.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Вход в систему — Кадры ПГЭК"
        Height="480" Width="820"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource AppBackgroundBrush}">

    <Grid Margin="40">
        <StackPanel VerticalAlignment="Center">

            <TextBlock Text="Кадры ПГЭК" 
                       FontSize="26" 
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Foreground="{StaticResource AccentGreenBrush}" />

            <TextBlock Text="Инспектор по кадрам" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Foreground="Gray" Margin="0,4,0,40"/>

            <TextBox materialDesign:HintAssist.Hint="Логин"
                     Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,8"/>

            <PasswordBox materialDesign:HintAssist.Hint="Пароль"
                         x:Name="PasswordBox"
                         PasswordChanged="PasswordBox_PasswordChanged"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,8"/>

            <TextBlock Text="{Binding ErrorMessage}" 
                       Foreground="#D32F2F" 
                       Margin="0,12"
                       HorizontalAlignment="Center"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>

            <Button Content="Войти в систему"
                    IsDefault="True"
                    Command="{Binding LoginCommand}"
                    Height="48"
                    Margin="0,20,0,0"
                    Background="{StaticResource AccentGreenBrush}"
                    Foreground="White"
                    FontSize="15"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>

            <ProgressBar IsIndeterminate="True"
                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                         Margin="0,15,0,0"
                         Height="4"/>
        </StackPanel>
    </Grid>
</Window>